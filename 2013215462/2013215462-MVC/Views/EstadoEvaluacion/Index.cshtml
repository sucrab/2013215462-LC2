@model IEnumerable<_2013215462_ENT.EstadoEvaluacion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EvaluacionID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EvaluacionID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EstadoEvaluacionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EstadoEvaluacionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EstadoEvaluacionID })
        </td>
    </tr>
}

</table>
