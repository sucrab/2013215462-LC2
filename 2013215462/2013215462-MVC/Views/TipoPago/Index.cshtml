@model IEnumerable<_2013215462_ENT.TipoPago>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VentaID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VentaID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoPagoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoPagoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoPagoID })
        </td>
    </tr>
}

</table>
