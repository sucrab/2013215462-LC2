@model IEnumerable<_2013215462_ENT.Distrito>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Provincia.CadenaUbigeo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CadenaUbigeo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Provincia.CadenaUbigeo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CadenaUbigeo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DistritoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DistritoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DistritoID })
        </td>
    </tr>
}

</table>
