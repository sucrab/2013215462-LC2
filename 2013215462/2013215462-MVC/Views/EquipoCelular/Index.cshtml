@model IEnumerable<_2013215462_ENT.EquipoCelular>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdministradorEquipo.AdministradorEquipoID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EvaluacionID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AdministradorEquipo.AdministradorEquipoID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EvaluacionID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EquipoCelularID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EquipoCelularID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EquipoCelularID })
        </td>
    </tr>
}

</table>
